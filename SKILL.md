---
name: ai-opportunity-assessment
description: 在完成客戶 kick-off 訪談後，將訪談內容整理成一份標準化的「AI 導入機會盤點表」，回答三件事：哪些任務值得導入 AI、預期省多少工時、建議從哪個開始做。適用任何產業（電商、B2B SaaS、傳產、專業服務業等），不綁定特定客戶。觸發語／場景範例：(1)「幫我把這份 kick-off 訪談整理成 AI 導入機會盤點」；(2)「這個客戶有哪些任務值得導入 AI？先做哪一個？」；(3)「產出一張 AI 導入優先級建議表給客戶決策」；(4) 使用者貼上訪談逐字稿或會議筆記，並要求評估 AI 導入機會。
---

# AI 導入機會盤點 Skill

## 用途

每接一個新客戶，kick-off 訪談後將聽到的內容整理成標準化的「AI 導入機會盤點表」，供客戶決策：哪些任務值得導入 AI、預期省多少工時、建議從哪個開始做。適用任何產業，判準固定、格式一致、可跨機會比較。

## 輸入格式

**必要輸入：**

- **訪談逐字稿或會議筆記**：kick-off 訪談的文字紀錄，格式不限（逐字稿、條列筆記皆可）
- **團隊規模**：受影響部門的人數，用於估算工時節省的總量級

**選填輸入：**

- **產業別與客戶背景**：一頁以內的公司簡介；缺少時 AI 會先嘗試從訪談內容推斷
- **現有工具清單**：CRM、ERP、客服系統等；影響導入難度評估
- **量化數據**：各任務的工時、件數、頻率；缺少時以產業基準估算並明標假設

## 步驟

1. **抽取任務與痛點**：通讀訪談內容，抽取所有「重複發生、有明確流程」的任務，並對應每個任務的痛點描述。每個抽取結果必須標註訪談原文出處（段落或發言者），不可自行腦補訪談中沒有的任務。
2. **分類 AI 任務類型**：將每個任務標記為生成型（產出文字／內容）、判斷型（分類／評分／排序）、或事實查詢型（檢索與彙整既有資訊），混合型需標明主次。
3. **估算工時節省**：有客戶量化數據時以「單件時間 × 件數 × AI 可承接比例」計算；無數據時以產業基準估算，並在該欄明確標註「估算值（假設：……）」，禁止把估算值寫成客戶實際數據。
4. **評估導入難度**：以兩個維度評分：資料就緒度（所需資料是否已數位化、可取得）與流程變動幅度（現有做事方式需要改多少）。兩者皆低＝難度低；任一為高＝難度高。
5. **排定優先級並寫理由**：以「工時節省 × 導入難度」矩陣排 P0／P1／P2：P0 必須同時滿足「難度為低或中」且「節省可量化」。每個 P0／P1 附一句「為什麼先做這個而不是另一個」的比較式理由。
6. **產出盤點表與待補清單**：輸出標準盤點表；若訪談資料不足以支撐某欄位，該欄不硬填，改列入表尾的「待補問題清單」供下次會議追問。

## 輸出格式

輸出一張「AI 導入機會盤點表」，每個機會點包含以下欄位：

| 欄位 | 說明 |
|---|---|
| 情境名稱 | 一句話任務名稱，客戶一看就懂 |
| 目前流程與痛點 | 現況怎麼做、卡在哪，附訪談出處 |
| AI 任務類型 | 生成型／判斷型／事實查詢型 |
| 預估工時節省 | 每週或每月節省時數；估算值需標註假設 |
| 導入難度 | 低／中／高（資料就緒度 × 流程變動幅度） |
| 建議優先級 | P0／P1／P2 ＋ 一句先做原因 |

表尾附「待補問題清單」（若有資料缺口）。

## 品質把關規則

1. 可追溯規則：盤點表中每一個機會點的「目前流程與痛點」都必須能對應到訪談原文的具體段落；若找不到出處，該機會點刪除，不得保留。
2. P0 門檻規則：任何 P0 機會點必須同時滿足「導入難度為低或中」且「工時節省欄有數字（實際或已標註假設的估算）」；不滿足者自動降為 P1，並在理由欄註明降級原因。
3. 假設透明規則：所有估算值必須以「估算值（假設：……）」格式標註，表中不得出現無法區分「客戶數據」與「顧問估算」的數字。

## 失敗回退

- **訪談資料不完整**：不硬猜。照常產出可支撐的機會點，其餘缺口整理成「待補問題清單」（含建議追問句），標明補齊後可解鎖哪些欄位。
- **看不出產業脈絡**：先向使用者要求一頁以內的客戶背景描述（產業、商業模式、主要部門）；若無法取得，以訪談中出現的職稱與術語推斷產業，並在盤點表開頭標註「產業推斷：……，待客戶確認」。
- **客戶沒給量化數據**：以產業基準與團隊規模反推估算，明標假設；同時在待補清單加入「請客戶提供 2 週的任務時間紀錄」作為下一步驗證動作。

## 手寫樣例

假設客戶：30 人電商公司，客服主管於 kick-off 反映「退換貨回覆太慢，旺季一個人一天要處理 60 件，寫回覆佔掉大半時間」。

| 情境名稱 | 目前流程與痛點 | AI 任務類型 | 預估工時節省 | 導入難度 | 建議優先級 |
|---|---|---|---|---|---|
| 退換貨回覆草稿生成 | 客服逐件手寫退換貨回覆信，每件約 6 分鐘；旺季單人日處理 60 件（訪談：客服主管發言） | 生成型 | 估算值（假設：AI 草稿＋人審每件縮短至 2 分鐘、3 位客服）：約每週 20 小時 | 低（歷史工單與回覆範本已在客服系統中，資料就緒；流程僅新增「審草稿」一步） | P0 — 資料現成、當週即可上線、節省可立即量測 |
| 退換貨原因自動分類分析 | 退換貨原因散落在信件與備註中，主管每月手動彙整報表約 8 小時（訪談：客服主管發言） | 判斷型 | 估算值（假設：AI 自動分類＋主管抽查）：約每月 6 小時 | 中（需先定義分類標準並累積標註樣本，涉及報表流程調整） | P1 — 價值在管理洞察，但需 P0 上線後累積結構化資料才能做準 |

**排序判斷邏輯**：為什麼 P0 是「回覆草稿生成」而不是「原因分類」：前者資料已就緒（歷史工單現成）、節省可即時量化驗證；後者需先累積標註資料且依賴前者產生的結構化紀錄，故後做。
